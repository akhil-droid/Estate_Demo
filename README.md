clone the repo

pip install -r requirements.txt

get your openai api key and paste in the .env file

Terminal 1 - Start server:
- cd backend
- python main.py

Terminal 2 - Run tests:
- python tests/test_scenarios.py
